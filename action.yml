name: TMScanner Scan
description: >
  Scan your sol files
branding:
  icon: check
  color: green
runs:
  using: docker
  image: Dockerfile
inputs:
  TM_URL:
    description: url of TMScanner scanner
    required: true
  TM_REPOSITORY_ID:
    description: TM_REPOSITORY_ID
    required: true
  TM_SOURCE:
    description: TM_SOURCE
    required: true